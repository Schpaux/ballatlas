# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
.next
dist
out
build

# Environment variables — never commit these
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Turbo
.turbo

# Vercel
.vercel

# Supabase local — generated files only, not migrations
supabase/.branches
supabase/.temp

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Testing
coverage

# OS
.DS_Store
Thumbs.db

# Editors
.idea
.vscode
*.swp
*.swo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Note: packages/database/src/types.generated.ts IS tracked in git.
# The stub ships with the repo so CI builds without needing supabase CLI.
# Regenerate with `pnpm supabase:types` after any schema change, then commit.
