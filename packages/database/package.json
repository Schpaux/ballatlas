{
  "name": "@ballatlas/database",
  "version": "0.0.1",
  "private": true,
  "description": "Supabase client, generated types, and environment validation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate-types": "supabase gen types typescript --local > src/types.generated.ts"
  },
  "dependencies": {
    "@ballatlas/types": "workspace:*",
    "@supabase/supabase-js": "^2.49.4",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@ballatlas/typescript-config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
