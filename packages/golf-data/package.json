{
  "name": "@ballatlas/golf-data",
  "version": "0.0.1",
  "private": true,
  "description": "Golf ball domain models, entities, and business logic — framework-agnostic",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ballatlas/types": "workspace:*",
    "@ballatlas/validators": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@ballatlas/typescript-config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
