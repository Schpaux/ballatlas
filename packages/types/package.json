{
  "name": "@ballatlas/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types — zero runtime dependencies",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@ballatlas/typescript-config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
